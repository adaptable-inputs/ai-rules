---
applies_to:
  load: "index"
  purpose: "the generated selection table for every loadable rule doc"
---
# Manifest

Generated by `scripts/gen_manifest.py`. Do not edit by hand; CI verifies it
against the `applies_to` frontmatter of every document.

An agent following the `AI.md` Loading Protocol reads this file once, then loads
only the rows whose condition matches the detected stack. It does not need to
open a category index.

- `always` rows load on every task.
- `conditional` rows load only when the condition holds.
- `task` rows load by task type, not by stack.
- A doc with an annex has a sibling `*.ANNEX.md`; load it only for the tasks its
  `tasks:` field names.

## always (20)

- `COMPLIANCE/DORA.md` *(annex)*
- `COMPLIANCE/EPRIVACY_TTDSG.md` *(annex)*
- `COMPLIANCE/EU_AI_ACT.md` *(annex)*
- `COMPLIANCE/GDPR_BDSG.md` *(annex)*
- `COMPLIANCE/LICENSES.md` *(annex)*
- `COMPLIANCE/NIS2_KRITIS.md` *(annex)*
- `CORE/CODE_REVIEW_PLATFORM.md` *(annex)*
- `CORE/CORE.md`
- `CORE/LOGGING.md` *(annex)*
- `CORE/NORMATIVE_LANGUAGE.md` *(annex)*
- `CORE/RULE_DEPENDENCY_TREE.md` *(annex)*
- `CORE/VERSION_CONTROL_SYSTEM.md`
- `DESIGN/CLEAN_CODE.md` *(annex)*
- `DESIGN/COGNITIVE_COMPLEXITY.md` *(annex)*
- `DESIGN/EARLY_RETURN.md` *(annex)*
- `DESIGN/SOLID.md` *(annex)*
- `LANGUAGE/CONVENTIONS.md` *(annex)*
- `LANGUAGE/READABILITY.md` *(annex)*
- `SECURITY/SECURITY.md` *(annex)*
- `TEST/TEST.md` *(annex)*

## conditional (70)

- `ARCHITECTURE/CIRCUIT_BREAKER.md` *(annex)* - when and how to apply circuit breakers to remote
  dependencies. Load when the project calls remote dependencies.
- `ARCHITECTURE/CLEAN_ARCHITECTURE.md` *(annex)* - dependency-direction and boundary rules for
  layered architecture. Load when the project layers domain against infrastructure.
- `ARCHITECTURE/CQRS.md` *(annex)* - rules for separating write behavior (commands) and read
  behavior (queries) when domain complexity or scale justifies it. Load when the project separates
  command and query models.
- `ARCHITECTURE/EVENT_DRIVEN_ARCHITECTURE.md` *(annex)* - event-driven architecture constraints for
  reliability, consistency, and operability. Load when the project publishes or consumes events.
- `ARCHITECTURE/GRAPHQL.md` *(annex)* - GraphQL schema and resolver architecture constraints. Load
  when the project exposes or consumes a GraphQL API.
- `ARCHITECTURE/MICROSERVICE.md` *(annex)* - when and how to use microservices safely. Load when the
  system is decomposed into services.
- `ARCHITECTURE/N_PLUS_1.md` *(annex)* - anti-N+1 rules across ORM, SQL, and GraphQL/resolver-based
  systems. Load when the project uses an ORM or lazy-loaded associations.
- `ARCHITECTURE/REST.md` *(annex)* - REST API design constraints for correctness, compatibility, and
  operability. Load when the project exposes or consumes a REST API.
- `BUILD_TOOLS/BUN.md` *(annex)* - Bun-specific dependency/install behavior and compatibility
  controls. Load when tools: `bun`.
- `BUILD_TOOLS/GRADLE.md` *(annex)* - Gradle-specific reproducibility, dependency/plugin, and CI
  controls. Load when tools: `gradle`.
- `BUILD_TOOLS/MAVEN.md` *(annex)* - Maven-specific reproducibility, dependency, and lifecycle
  controls. Load when tools: `maven`.
- `BUILD_TOOLS/NPM.md` *(annex)* - reproducible dependency-management and script-execution rules.
  Load when tools: `npm`.
- `CI-CD/GITHUB_ACTIONS.md` *(annex)* - GitHub Actions pipeline design and quality-gate constraints.
  Load when tools: `github-actions`.
- `CI-CD/GITLAB.md` *(annex)* - GitLab pipeline design and quality-gate constraints. Load when
  tools: `gitlab-ci`.
- `CORE/CONFLUENCE.md` *(annex)* - Confluence-specific write/delete safety constraints. Load when
  tools: `confluence`.
- `CORE/GITHUB.md` *(annex)* - GitHub-specific delivery and review workflow constraints. Load when
  tools: `github`.
- `CORE/GITLAB.md` *(annex)* - GitLab-specific delivery and review workflow constraints. Load when
  tools: `gitlab`.
- `CORE/JIRA.md` *(annex)* - Jira-specific ticket-authoring, update, and summary-output rules. Load
  when tools: `jira`.
- `DESIGN/AOP.md` *(annex)* - when AOP is appropriate for cross-cutting concerns. Load when the
  project applies aspect-oriented cross-cutting concerns.
- `DESIGN/GOF_DESIGN_PATTERNS.md` *(annex)* - when GoF patterns are useful and when they are
  overengineering. Load when the task involves selecting a design pattern.
- `FRAMEWORK/ANGULAR.md` *(annex)* - Angular-specific component, template, reactive-state, and
  runtime rules. Load when frameworks: `angular`.
- `FRAMEWORK/IONIC.md` *(annex)* - Ionic-specific UI, navigation, native-bridge, and mobile-delivery
  guardrails. Load when frameworks: `ionic`.
- `FRAMEWORK/PRIMEFACES.md` *(annex)* - PrimeFaces-specific rules for JSF lifecycle, state handling,
  and UI behavior. Load when frameworks: `primefaces`.
- `FRAMEWORK/REACT.md` *(annex)* - React-specific component, rendering, and side-effect rules. Load
  when frameworks: `react`.
- `FRAMEWORK/SPRING_BOOT.md` *(annex)* - Spring Boot-specific defaults for layering, configuration,
  and runtime behavior. Load when frameworks: `spring-boot`.
- `FRAMEWORK/SVELTE.md` *(annex)* - Svelte-specific component, reactivity, and state-management
  rules. Load when frameworks: `svelte`.
- `FRAMEWORK/TAILWIND_CSS.md` *(annex)* - Tailwind-specific rules for scalable, readable, and
  accessible styling. Load when frameworks: `tailwind`.
- `INFRASTRUCTURE/ANSIBLE.md` *(annex)* - Ansible-specific rules for safe, repeatable infrastructure
  and configuration automation. Load when tools: `ansible`.
- `INFRASTRUCTURE/AWS.md` *(annex)* - AWS-specific platform guardrails for secure and reliable
  operations. Load when tools: `aws`.
- `INFRASTRUCTURE/AZURE.md` *(annex)* - Azure-specific platform guardrails for secure and reliable
  operations. Load when tools: `azure`.
- `INFRASTRUCTURE/CLOUDFORMATION.md` *(annex)* - CloudFormation-specific rules for safe and
  reproducible AWS infrastructure delivery. Load when tools: `cloudformation`.
- `INFRASTRUCTURE/DOCKER.md` *(annex)* - secure, reproducible, and efficient container build/runtime
  rules. Load when tools: `docker`.
- `INFRASTRUCTURE/GCP.md` *(annex)* - GCP-specific platform guardrails for secure and reliable
  operations. Load when tools: `gcp`.
- `INFRASTRUCTURE/HELM.md` *(annex)* - Helm chart authoring, templating, and release management
  rules. Load when tools: `helm`.
- `INFRASTRUCTURE/INFRA_AS_CODE.md` *(annex)* - tool-neutral IaC guardrails for provisioning and
  evolving infrastructure safely. Load when the project declares any infrastructure as code.
- `INFRASTRUCTURE/ISTIO.md` *(annex)* - Istio traffic, security, and observability policy defaults.
  Load when tools: `istio`.
- `INFRASTRUCTURE/KUBERNETES.md` *(annex)* - Kubernetes workload, deployment, and runtime safety
  defaults. Load when tools: `kubernetes`.
- `INFRASTRUCTURE/PULUMI.md` *(annex)* - Pulumi-specific rules for predictable and safe
  infrastructure provisioning. Load when tools: `pulumi`.
- `INFRASTRUCTURE/TERRAFORM.md` *(annex)* - Terraform-specific rules for predictable and safe
  infrastructure delivery. Load when tools: `terraform`.
- `LANGUAGE/CSS/CSS.md` *(annex)* - CSS baseline rules for predictable, maintainable, and accessible
  UI styling. Load when languages: `css`.
- `LANGUAGE/C_SHARP/C_SHARP.md` *(annex)* - baseline C#/.NET rules for correctness, maintainability,
  and runtime safety. Load when languages: `csharp`.
- `LANGUAGE/GO/GO.md` *(annex)* - baseline Go rules for correctness, maintainability, and production
  safety. Load when languages: `go`.
- `LANGUAGE/HTML/HTML.md` *(annex)* - semantic, accessible, and secure HTML defaults. Load when
  languages: `html`.
- `LANGUAGE/JAVA/EFFECTIVE_JAVA.md` *(annex)* - Effective Java preference heuristics that narrow the
  Java baseline. Load when languages: `java`.
- `LANGUAGE/JAVA/JAVA.md` *(annex)* - the baseline Java ruleset for correctness, maintainability,
  and interoperability. Load when languages: `java`.
- `LANGUAGE/JAVASCRIPT/JAVASCRIPT.md` *(annex)* - a complete JavaScript baseline for runtime-safe,
  maintainable code. Load when languages: `javascript`.
- `LANGUAGE/KOTLIN/KOTLIN.md` *(annex)* - baseline Kotlin rules for correctness, maintainability,
  and runtime safety. Load when languages: `kotlin`.
- `LANGUAGE/PHP/PHP.md` *(annex)* - baseline PHP rules for correctness, maintainability, and
  production safety. Load when languages: `php`.
- `LANGUAGE/PYTHON/PYTHON.md` *(annex)* - baseline Python rules for correctness, maintainability,
  and production safety. Load when languages: `python`.
- `LANGUAGE/RUBY/RUBY.md` *(annex)* - baseline Ruby rules for correctness, maintainability, and
  production safety. Load when languages: `ruby`.
- `LANGUAGE/RUST/RUST.md` *(annex)* - baseline Rust rules for correctness, maintainability, and
  runtime safety. Load when languages: `rust`.
- `LANGUAGE/SHELL/SHELL.md` *(annex)* - safe and maintainable shell scripting defaults. Load when
  languages: `shell`.
- `LANGUAGE/SQL/SQL.md` *(annex)* - SQL correctness, safety, and performance baseline. Load when
  languages: `sql`.
- `LANGUAGE/SWIFT/SWIFT.md` *(annex)* - baseline Swift rules for correctness, maintainability, and
  runtime safety. Load when languages: `swift`.
- `LANGUAGE/TYPESCRIPT/TYPESCRIPT.md` *(annex)* - TypeScript-specific rules that specialize
  JavaScript guidance. Load when languages: `typescript`.
- `LANGUAGE/YAML/YAML.md` *(annex)* - YAML authoring rules for correctness, readability, and secure
  configuration management. Load when languages: `yaml`.
- `LIBRARY/GUAVA.md` *(annex)* - when to use Guava utilities versus JDK/native alternatives. Load
  when libraries: `guava`.
- `LIBRARY/JAVA_MONEY.md` *(annex)* - mandatory modeling and arithmetic rules for monetary values.
  Load when libraries: `javamoney`.
- `LIBRARY/JAXB.md` *(annex)* - JAXB usage rules for safe and stable XML
  serialization/deserialization. Load when libraries: `jaxb`.
- `LIBRARY/JEST.md` *(annex)* - Jest-specific rules for deterministic, maintainable tests. Load when
  libraries: `jest`.
- `LIBRARY/JOOQ.md` *(annex)* - jOOQ usage rules for type-safe SQL and predictable query behavior.
  Load when libraries: `jooq`.
- `LIBRARY/JPA.md` *(annex)* - JPA mapping and query usage rules for correctness and performance.
  Load when libraries: `jpa`.
- `LIBRARY/JUNIT.md` *(annex)* - JUnit-specific testing patterns for reliable, readable tests. Load
  when libraries: `junit`.
- `LIBRARY/KAFKA.md` *(annex)* - topic, producer, and consumer design constraints for reliable event
  streaming. Load when libraries: `kafka`.
- `LIBRARY/LOMBOK.md` *(annex)* - when Lombok improves clarity and when explicit code is safer. Load
  when libraries: `lombok`.
- `LIBRARY/MAPSTRUCT.md` *(annex)* - MapStruct mapping rules for explicit, type-safe DTO/entity
  conversion. Load when libraries: `mapstruct`.
- `LIBRARY/MOCKITO.md` *(annex)* - Mockito usage rules for behavior isolation without brittle tests.
  Load when libraries: `mockito`.
- `LIBRARY/PLAYWRIGHT.md` *(annex)* - Playwright E2E/integration testing rules for reliability and
  maintainability. Load when libraries: `playwright`.
- `LIBRARY/RESILIENCE4J.md` *(annex)* - safe usage of resilience primitives for remote dependency
  protection. Load when libraries: `resilience4j`.
- `LIBRARY/SELENIUM.md` *(annex)* - Selenium E2E automation patterns for stable browser testing.
  Load when libraries: `selenium`.

## task (3)

- `PLAN/PLAN.md` *(annex)* - planning standards for implementation tasks. Load when the task is
  `plan`.
- `PROGRAMMING/PROGRAMMING.md` *(annex)* - end-to-end implementation workflow expectations for
  coding tasks. Load when the task is `programming`.
- `REVIEW/CODE_REVIEW.md` *(annex)* - review workflow, prioritization, and output expectations. Load
  when the task is `review`.
